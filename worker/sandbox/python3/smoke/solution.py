# Minimal program used by `make verify-language LANG=python3` to confirm that
# the language's Dockerfile produces a working interpreter through
# IsolateEngine. Filename must match languages.yaml `source.filename`.
print("hello python3")
