apiVersion: v1
kind: Service
metadata:
  name: {{ include "common-worker.fullname" . }}
  labels:
    {{- include "common-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "common-worker.selectorLabels" . | nindent 4 }}
